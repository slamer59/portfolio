
const EMULATION_METRICS = {
  mobile: true,
  width: 412,
  height: 767,
  deviceScaleFactor: 1.75,
  disabled: false,
};


const baseUrl = "https://thomas.pedot.cc";
export default {
  // example
  site: baseUrl, // The site that will be scanned
  root: process.cwd(), // The path to the app that represents the site
  cache: false, // Should reports be saved to the local file system and re-used between runs
  configFile: null, // Load the configuration from a custom config file (unlighthouse.config.ts by default)
  outputPath: "./lighthouse-prod/", // Where to emit Lighthouse reports and the runtime client
  debug: false, // Display debug messages
  auth: false, // Optional basic auth credentials
  cookies: false, // Provide cookies to be set for Axios and Puppeteer requests
  extraHeaders: false, // Provide extra headers to be set for Axios and Puppeteer requests
  defaultQueryParams: false, // Query params to add to every request
  hooks: {}, // Hooks to run
  routerPrefix: "", // The URL path prefix for the client and API to run from
  apiPrefix: "/api/", // The path that the API should be served from
  urls: [
    `${baseUrl}`,
    `${baseUrl}/apropos`,
    `${baseUrl}/projets`,
    `${baseUrl}/photographie`,
    `${baseUrl}/articles`,
    `${baseUrl}/articles/renovate-code-dependencies-with-gitlab`,
  ],
  scanner: {
    customSampling: {}, // Custom mappings for regex strings to route definitions
    ignoreI18nPages: true, // Skip i18n copies of pages
    maxRoutes: 200, // Maximum number of routes to process
    include: null, // Paths to explicitly include from the search
    exclude: null, // Paths to ignore from scanning
    skipJavascript: true, // Whether JavaScript needs to be executed for internal links and SEO data
    samples: 1, // How many samples of each route should be done
    throttle: true, // Should Lighthouse run with throttling enabled
    crawler: true, // Should the crawler be used to detect URLs
    dynamicSampling: 5, // Dynamic sampling limit for redundant route reports
    robotsTxt: true, // Should the robots.txt file be used for configuration
    sitemap: true, // Enable sitemap scanning
  },
  chrome: {
    // forces the fallback to be used
    useSystem: false
  },
  lighthouseOptions: {
    formFactor: "mobile",
    throttling: {
      rttMs: 40,
      throughputKbps: 1638.4, // Network throughput (Simulated)
      cpuSlowdownMultiplier: 1,
      requestLatencyMs: 0, // 0 means unset
      // downloadThroughputKbps: 0,
      // uploadThroughputKbps: 0,
      latencyMs: 150, // Network latency (Simulated)
      offline: false, // Network is online
      downloadThroughputKbps: 1638.4, // Download throughput (Simulated)
      uploadThroughputKbps: 1638.4, // Upload throughput (Simulated)
      connectionType: "online", // Network connection type
    },
    screenEmulation: EMULATION_METRICS,
    lighthouseVersion: "11.0.0", // Use Lighthouse version 11.0.0    
    emulatedUserAgent: "Mozilla/5.0 (Linux; Android 11; moto g power (2022)) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/94.0.4606.85 Mobile Safari/537.36", // User agent for Moto G Power
  }

};
